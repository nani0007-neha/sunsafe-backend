services:
  - type: web
    name: sunsafe-web
    env: python
    rootDir: sunsafe
    buildCommand: pip install -r ../requirements.txt
    startCommand: gunicorn sunsafe.wsgi:application --bind 0.0.0.0:10000
    envVars:
      - key: PORT
        value: 10000
